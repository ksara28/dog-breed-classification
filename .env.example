# Copy this file to .env and replace the values below with your keys
# Do NOT commit your real .env file to version control.

# OpenAI API key for the /chat endpoint (optional)
OPENAI_API_KEY=REPLACE_WITH_YOUR_OPENAI_API_KEY

# Optional: backend base URL used by the frontend when development uses a different host/port
# VITE_API_BASE=http://localhost:5000
